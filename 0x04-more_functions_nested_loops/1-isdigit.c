#include "main.h"

/**
 * _isdigit -> this is a function to check for a digit
 * @c: an interger parameter
 * Return: something but success
*/

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
