#include "main.h"

/**
 * mul -> this is a function to multiply 2 integers
 * @x: an integer parameter1
 * @y: an integer parameter2
 * Return: success and an integer
*/

int mul(int x, int y)
{
	return (x * y);
}
