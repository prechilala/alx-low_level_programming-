#include "main.h"
#include <string.h>

/**
 * _strcmp -> function to compare
 * @s1: first parameter
 * @s2: second parmeter
 * Return: a integer
*/

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
